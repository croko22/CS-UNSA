#include "OctTree.h"
#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    OctTree *tree = new OctTree();
    // Point *p = new Point(1, 2, 3);
    // tree->insert(p);

    cout << "HI" << endl;
    return 0;
}
